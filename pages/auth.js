import React from "react";
import FirebaseAuth from "../components/FirebaseAuth";

const Auth = () => {
    return (
        <div>
            <div>
                <FirebaseAuth />
            </div>
        </div>
    );
};

Auth.propTypes = {};

export default Auth;
